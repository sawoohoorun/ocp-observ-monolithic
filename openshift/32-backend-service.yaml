apiVersion: v1
kind: Service
metadata:
  name: backend-app
  namespace: dev
  labels:
    app.kubernetes.io/name: backend-app
    app.kubernetes.io/part-of: dev
spec:
  selector:
    app.kubernetes.io/name: backend-app
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
