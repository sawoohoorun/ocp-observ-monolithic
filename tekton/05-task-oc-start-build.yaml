---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: oc-start-build-wait
  namespace: observability-single-span
  labels:
    app.kubernetes.io/part-of: observability-single-span
spec:
  description: Run OpenShift binary build for a BuildConfig and wait for completion (Option A — cluster BuildConfig owns the image).
  stepTemplate:
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
      runAsNonRoot: true
      seccompProfile:
        type: RuntimeDefault
  params:
    - name: namespace
      type: string
      default: observability-single-span
    - name: buildconfig
      type: string
    - name: commit
      description: If non-empty, passed to oc start-build --commit (must exist in the BuildConfig Git history)
      type: string
      default: ""
  steps:
    - name: start-build
      image: registry.redhat.io/openshift4/ose-cli:latest
      script: |
        #!/bin/bash
        set -euo pipefail
        NS="$(params.namespace)"
        BC="$(params.buildconfig)"
        REV="$(params.commit)"
        if [ -n "${REV}" ]; then
          oc start-build "${BC}" -n "${NS}" --commit="${REV}" --wait --follow
        else
          oc start-build "${BC}" -n "${NS}" --wait --follow
        fi
